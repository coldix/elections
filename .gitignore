node_modules/
.DS_Store
.env
.env.*

# Incremental lead-scan cache (hashes, seen fingerprints, last digest)
.cache/

# Build artefacts. The YAML in data/ is the source of truth; everything below
# is regenerated by `npm run build`.
dist/
.astro/
site/.astro/
site/public/data/

<style is:global>
  .pm-view .pm-issue-name,
  .pm-view .pm-card-head h3 > a {
    display: inline-flex;
    width: fit-content;
    align-items: center;
    gap: 0.38rem;
    padding: 0.42rem 0.72rem;
    border: 1px solid var(--accent);
    border-radius: 999px;
    background: var(--accent-wash);
    color: var(--accent-ink) !important;
    font-weight: 750;
    line-height: 1.25;
    text-decoration: none !important;
    box-shadow: 0 1px 2px rgb(22 22 29 / 0.08);
    transition:
      background 0.15s ease,
      color 0.15s ease,
      border-color 0.15s ease,
      transform 0.15s ease,
      box-shadow 0.15s ease;
  }

  .pm-view .pm-issue-name::after,
  .pm-view .pm-card-head h3 > a::after {
    content: "→";
    flex: none;
    font-size: 0.88em;
    line-height: 1;
  }

  .pm-view .pm-issue-name:hover,
  .pm-view .pm-card-head h3 > a:hover {
    border-color: var(--accent);
    background: var(--accent);
    color: var(--paper) !important;
    text-decoration: none !important;
    transform: translateY(-1px);
    box-shadow: var(--shadow);
  }

  .pm-view .pm-issue-name:active,
  .pm-view .pm-card-head h3 > a:active {
    transform: translateY(0);
  }

  @media (max-width: 58rem) {
    .pm-view .pm-card-head h3 > a {
      padding: 0.48rem 0.78rem;
    }
  }
</style>
